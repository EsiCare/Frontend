import { RouteProtectDirective } from './route-protect.directive';

describe('RouteProtectDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteProtectDirective();
    expect(directive).toBeTruthy();
  });
});
